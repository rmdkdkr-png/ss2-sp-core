# 월화의 검사 — 원버튼 필살기(SP) 이식 · M0 정찰

기획서는 세션 계획 파일에 있다. 이 문서는 **잰 것**만 적는다.
안 잰 것은 「미측정」이라고 쓴다 — 아는 척한 줄 하나가 다음 사람을 태운다.

## 준비물

| 것 | 값 |
|---|---|
| 하네스 | `/mnt/c/Claude/KOF R2 한글/tools/ngprun` + 코어 `/home/dudu/m1/m31.so` |
| 롬(대조군) | `~/ss2/rom/pristine/Last Blade, The (UE) [!].ngc` (sha `3195add4…`) |
| 세이브 | `~/ss2/saves/lb/lb_psel.st` · `lb_train.st` (`lb_mkstate.py` 로 재생성) |
| 코어 옵션 | `NGP_OPTS="ngp_ss2sp=enabled"` — 이게 꺼지면 `update_input` 이 통째로 안 돈다 |

⚠ **`~/ss2/rom/lastblade.ngc` 는 순정이 아니다**(sha `ca8cf4cf…`, 한글판). 대조군으로 쓰지 마라.
처음에 그걸로 부팅해서 한글 인트로가 떴다 — 롬 이름만 보고 순정이라 여긴 것이 잘못이었다.

## 트레이닝 무대로 가는 길 (실측)

```
자연 부팅 4500프레임 → 제목 「PRESS A BUTTON」
  A → 1P PLAY 메뉴
  A → 모드 선택 (STORY / SURVIVAL / TIME ATTACK / TRAINING)
  ↓ ×3 → TRAINING
  A → Player Select   ← lb_psel.st 를 여기서 뜬다
  A → (P1 확정)
  A → **Ability Select**
  A → (P2 캐릭)
  A → (P2 확정)
  → 무대                ← lb_train.st
```

- **버튼 문자는 레트로패드 기준**이다: NGP A = 대본 `B` · NGP B = 대본 `A`.
- **트레이닝 모드가 있다는 것이 이번 이식의 제일 큰 이득이다.** SvC 는 스파링 무대를 억지로
  구웠고 애니 뱅크가 안 갱신돼 자동 실측에 못 썼다. 월화는 게임이 허수아비를 준다.
- 게임이 화면에 **「DAMAGE %」와 히트 수**를 띄운다 — 교차 증인으로 쓸 수 있다.
- ⚠ **Ability Select 가 흐름 안에 있다**(POWER 표기 확인). 속성·장비가 기술표를 흔드는지는
  **미측정**이다. 슬롯을 짜기 전에 반드시 가른다.
- ⚠ Player Select 에서 **다섯 번** 눌러야 무대다. 두 번만 누르고 「무대」라 이름 붙였다가
  Ability Select 를 찍었다. **화면을 한 칸씩 찍어 확인해라.**

## ★ 방향이력 링 — 찾았다

```
링    0x1313 + (n & 0x7F)     n = 0…127   (128칸, 1바이트 간격)
머리  0x1312                  다음에 쓸 칸의 색인
한 칸 2프레임                  → 256프레임을 기억한다
값    NGP 패드 비트 그대로      U=1 D=2 L=4 R=8 · NGP A=16 · NGP B=32
P2    +0x88 (머리 0x139A, 링 0x139B~0x141A)
```

**SvC·KOF 와 구조가 다르다.** 그 둘은 값이 **밀린다**(간격 2). 월화는 **값이 안 움직이고
머리만 나아간다**(간격 1). 결과는 같지만 주입 코드가 다르다 — 밀기를 흉내 내면 안 된다.

근거:
- 앞을 4프레임 잡으면 f2 에 0x135B, f4 에 0x135C 에 `8` 이 앉는다 — **2프레임에 한 칸**.
- 앞4 → 중립10 → 뒤4 를 넣으면 `4` 가 0x135D 가 아니라 **0x1362** 에 앉는다.
  사이 다섯 칸이 0 으로 남는다 → **중립도 기록된다.** 칸 수가 프레임 수와 정확히 맞는다.
- R·L·D·U 넷 다 「머리 72 → 0x1313+72 = 0x135B 에 값이 앉고 머리가 73 이 된다」.
- 한 방향을 600프레임 잡으면 `0x1313~0x1392` 128칸이 전부 그 값이 된다 — 그 이상 안 번진다.
- 머리를 중립으로 굴리면 124 → 16 으로 돌아온다(40프레임=20칸, 124+20−128=16) → **128에서 되돌아온다.**

⚠ **한 칸만 poke 로 붙잡아 증명하려 하지 마라.** KOF 에서 링을 정적으로 붙잡았더니 게임이
읽는 시작점이 돌아 사실상 모든 회전을 시도하게 돼 증명이 안 됐다. **한 번만 쓰는 것**이 본질이다.

## 램 후보 (아직 확정 아님)

| 주소 | 무엇으로 보이나 | 쉴 때 | 근거 |
|---|---|---|---|
| **0x0370** | **동작 ID(act) — 승격함** | **4** | 서서 베기 96 · 앉아 베기 120 · 킥 128 · 236+A 112 · 623+A 116 · 214+B 168. **위상 2종 일치 · 기술 여섯으로 교차 확인** (MOVES.md) |
| 0x037C | 공격 판정 구간? | **255** | 기술이 나가는 동안만 255 를 벗어난다(서서A 52→32, 앉아A 80→28) |
| 0x0F60/61 | **애니 대본 커서(16비트)** | 0x4FC6 | 서서A 0x56FD · 앉아A 0x3B5B. SvC 0x0958·KOF 0x0D78 에 해당 |
| 0x0F5B · 0x0F64 | 애니 번호 | 78 | 둘이 늘 같이 움직인다 |
| 0x036A · 0x03EA | 가로 위치? | 120 | 앞 201 · 뒤 56. **둘이 값이 같다** — 하나는 사본이거나 카메라다 |

**0x0370 은 승격했다** — 기술 여섯을 위상 2종으로 돌려 값이 전부 같았고, **표에 없는 커맨드(421+A)는
평타만 나오는** 대조군이 섰다. 자세한 것은 .

나머지 넷(0x037C · 0x0F60/61 · 0x0F5B · 0x036A)은 **아직 후보**다 — 위상 2종을 안 돌렸다.
그 전에는 코드에 값을 박지 마라.

**아직 못 찾은 것**: 콤보 카운터 · 상대 체력 · **좌우 반전** · 캐릭터 id.
⚠ 반전은 특히 조심해라 — KOF 는 `0x0D4A` 를 반전으로 잘못 잡았는데 그건 「필살기를 한 번
쓰면 서고 안 내려오는 플래그」였고, 배포된 엔진이 **한 라운드 두 번째 발동부터 커맨드를
좌우로 뒤집었다.** 관문 40/40·98/98 이 전부 초록이었던 이유는 **시행마다 세이브를 다시
불러왔기 때문**이다.

## 함정 (이번에 밟은 것)

1. **`!w` 의 오프셋 목록은 159자에서 잘린다**(`ngprun.c` 의 `%159s`). 30칸이 한계다.
   64칸을 주고 24칸만 받아 「링이 없다」로 읽을 뻔했다.
2. **`!w` CSV 열은 `frame,pad,<오프셋…>`** 이다. `pad` 를 첫 오프셋으로 읽으면 전부 한 칸 밀린다.
3. **`30 D` 뒤에 `4 B` 는 앉아 베기가 아니다** — 방향을 떼고 친 것이다. `12 D B` 로 같이 잡아라.
4. **잡음 바이트 0개**다. SvC(9)·SS2(3)와 달리 이 무대는 결정적이라 두 번 뜨기가 싸다.
   그래도 **거르는 절차는 남겨 둔다** — 무대가 바뀌면 다시 생긴다.


---

# M1 · 배관만 — **통과** (2026-09-06)

신설: `src/lbsp.c` · `src/lbsp.h` · `tools/lb/test_lbsp.c`
배관: `build/Makefile.common` 에 `lbsp.c` · `build/libretro.c` 에 extern·훅·가지

`lbsp_frame` 은 **순정 롬 폴드와 글자 그대로 같다**(Y=A · X=B · L·R=A+B).
일부러 아무것도 안 한다 — 여러 게임을 한 `.so` 에 담을 때 실제로 깨지는 자리는
**분기와 배관**이지 엔진이 아니기 때문이다.

## 관문 ① 무회귀 — 일곱 게임 전부 0바이트

`python3 tools/kof/m1_reg.py /tmp/core_base.so /tmp/core_new.so`
(기준 = 직전 커밋 96f2925 를 워크트리에서 따로 구운 것)

| 게임 | 램 | 화면 | 소리 |
|---|---|---|---|
| svc | 0/0/0 | 0/0/0 | (잡음 — 판정 안 함) |
| ss2 | 0/0/0 | 0/0/0 | 같음 |
| mslug1 · mslug2 · ffury | 0/0/0 | 0/0/0 | 같음 |
| kofr2 | 0/0/0 | 0/0/0 | 같음 |
| **lb** | **0/0/0** | **0/0/0** | 같음 |

같이 고친 것: `m1_reg.py` 의 GAMES 에 **월화 추가**, 그리고 메탈슬러그·아랑 롬 경로.
전에는 그 셋이 「롬 없음」으로 **조용히 건너뛰어** 다섯을 쟀다고 착각할 수 있었다.

## 관문 ② 폴드 전수 — 8,192조합 어긋남 0

```
cd tools/lb && cc -O1 -I../../src -o /tmp/test_lbsp test_lbsp.c ../../src/lbsp.c && /tmp/test_lbsp
```
pad 256 × (Y,X,L,R) 16 × 엔진 켬/끔 2 = **8,192**. 어긋남 0.
롬 판별 진리표도 같이 본다 — LASTBLADE124 만 물고 SAMURAI2·KOF R2·SNKvsCAPCOM1·**GEKKA**
는 안 문다(J판이 범위 밖인 것은 의도다).

에뮬로는 이 둘을 **못 가른다** — 출력이 같아서다. 그래서 단위 시험이 따로 있다.

## ⚠ 이번에 데인 것 — `build.sh` 가 `build/` 를 되돌린다

`build/` 는 별도 클론이 **아니라 이 저장소의 일부**다. `build.sh` 첫머리의
`git reset --hard <upstream커밋>` 은 그 커밋이 이 저장소에 없어 실패하고,
뒤따르는 `|| git checkout -- .` 이 **`build/` 의 안 커밋한 편집을 통째로 되돌린다.**

실제로 당했다: 배관을 넣고 바로 구웠더니 링크줄에 `lbsp.o` 가 없었다.
**`build/` 를 고쳤으면 굽기 전에 커밋해라.** 그리고 `tools/core/regen_patch.sh` 로
`src/ss2sp.patch` 를 갱신해라 — 그게 upstream 대비 정본이다.

## 다음 (M2)

카에데 236+A 하나를 매크로로. 관문은 위상 2종 각각 ≥19/20 · 엔진 끔 대조군 0/n,
그리고 **연속 발동**(세이브를 다시 안 불러오고 한 상태에서 여러 번). KOF 는 M5 까지
전부 초록이었는데도 배포 사고가 났고, 원인이 시행마다 세이브를 다시 부른 것이었다.

M2 에서 **R 이 트리거가 되며 폴드에서 빠진다** — 그때 출력이 처음 달라진다.
`test_lbsp.c` 의 `ref_fold` 를 **지우지 말고 새 계약으로 고쳐라**(kofsp 가 그렇게 했다).


---

# M2 · 매크로 하나 — **통과** (2026-09-06)

트리거 **레트로패드 R** 한 번에 카에데 **236+A(질풍, act 112)** 가 나간다.
매크로는 패드로 걸어 넣는다(D 4f → D+R 4f → R 4f → A 6f, 모두 18프레임).
**링은 아직 안 쓴다** — 그건 M3 다. 여기서 링까지 같이 넣으면 실패했을 때
**배관 탓인지 링 탓인지 못 가른다.**

## 관문 — `python3 tools/lb/lb_m2gate.py`

```
위상 0 · 엔진 켬 : 20/20
위상 1 · 엔진 켬 : 20/20
     엔진 끔 대조군:  0/20
연속 발동(세이브 안 불러옴) 5/5 : 기술 기술 기술 기술 기술
판정: PASS
```

**연속 발동을 여기서부터 넣었다.** 세이브를 다시 안 불러오고 한 상태에서 다섯 번.
KOF 는 M5(98/98)까지 전부 초록이었는데도 배포 사고가 났고, 원인이 **시행마다
세이브를 다시 부른 것**이었다. 마지막이 아니라 처음부터 본다.

⚠ **대조군의 한계를 적어 둔다.** 엔진 끔 + 1프레임 트리거는 **아무 일도 안 일어난다**
(짧아서 게임 폴링에 안 걸린다). 6프레임으로 길게 누르면 엔진 끔에서 **킥(act 128)**이
나온다 — R 이 A+B 로 접히기 때문이다. 즉 대조군이 증명하는 것은
「엔진이 없으면 **기술(112)이 안 나간다**」이지 「R 이 아무것도 안 한다」가 아니다.

## 단위 시험 — `test_lbsp.c` (M2 계약으로 고침)

- 폴드 **6,144조합** 어긋남 0 (엔진 켬 + R 누름은 트리거라 여기서 빼고 따로 본다)
- **매크로 18프레임 시간표**가 비트까지 일치 — 에뮬로는 못 가르는 것이다
- 트리거를 **쥐고 있어도** 두 번째가 저절로 안 나간다(누출 없음)
- **기술이 나가는 중에는 트리거가 안 먹는다**(act≠4 이면 안 건다)

## ★ 계약이 바뀌었다 — 엔진 끔은 «순정 그대로»

M1 은 엔진 상태와 무관하게 순정 폴드였다. M2 부터 —

| | Y | X | L | **R** |
|---|---|---|---|---|
| 엔진 **끔** | A | B | A+B | **A+B** (순정과 동일) |
| 엔진 **켬** | A | B | A+B | **트리거** (안 접힘) |

**kofsp 와 일부러 다르다** — kofsp 는 R 을 무조건 뺀다. 이렇게 해야 「엔진 끔」이
**글자 그대로의 대조군**이 된다. 대조군이 조금이라도 다르면 그건 대조군이 아니다.

## ⚠ 이번에 데인 것 — 「무반응」이라 적힌 것이 실은 「못 쟀다」였다

첫 관문이 **전부 무반응**으로 나왔다. 대조군까지 무반응이라 이상해서 재는 쪽을 먼저 봤다:
csv 파일 이름이 `g_t.csv` 가 아니라 **`gt.csv`**(접두사+태그를 그냥 붙인다)였다.
파일을 못 찾아 빈 목록이 돌아왔고, 내 판정 함수가 그것을 **「무반응」이라 불렀다.**

그대로 믿었으면 멀쩡한 엔진을 뜯어고치러 갔다. 지금은 「못잼」으로 갈라 놓았고,
**못 잰 시행이 하나라도 있으면 PASS 를 못 준다.**

## 다음 (M3)

1. **반전(좌우) 주소를 먼저 재라.** 지금 매크로는 앞=`NGP_R` 로 못 박혀 있어
   **P1 이 왼쪽인 트레이닝 무대에서만** 옳다. KOF 는 반전을 잘못 잡아
   한 라운드 두 번째 발동부터 커맨드가 좌우로 뒤집히는 사고를 배포까지 냈다.
2. 링 주입(`0x1313~`, 머리 `0x1312`)으로 18프레임 지연을 줄인다.
   ★ **월화 링은 값이 안 밀린다 — 머리만 나아간다.** SvC·KOF 의 밀기를 흉내 내지 마라.
3. 슬롯 일곱(N/F/B/D/DF/DB/AIR).


---

# M3 ① · 반전(좌우) — **승격** `0x0386` (2026-09-06)

**0 = 오른쪽 봄(앞=R) · 1 = 왼쪽 봄(앞=L).** P2 는 **+0x40**(P2 act 0x03B0, 쉼 4).

계획서의 접는 조건이 「반전을 못 확정하면 멈춘다」였다. 확정했다.

## 승격 근거 — 사다리 넷

**① 독립 시나리오 둘**

| | 어떻게 | 결과 |
|---|---|---|
| ⓐ 자리 바꾸기 | 앞으로 걷다 뛰어넘는다 | 반전 0 → **1**, 되넘으면 **0 으로 복귀** |
| ⓑ 걷는 방향 | 넘어간 뒤 R·L 을 잡아 본다 | 반전0: R=앞걷기20 · L=뒤걷기24<br>반전1: R=**24** · L=**20** — 네 칸이 딱 뒤집힌다 |

ⓑ 는 ⓐ 와 **성질이 다른 증인**이다(위치가 아니라 act 로 잰다).

**② 위상 2종** — 두 시나리오 모두 위상 0·1 이 프레임까지 완전히 같다.

**③ 교차 증인** — P2(`+0x40`)가 **늘 반대 값**이다. 넘어가는 동안만 잠깐 같아지는데
(P2 는 위치가 지나는 f98 에, P1 은 착지하는 f142 에 뒤집힌다) 그것도 사리에 맞는다.

**④ 대조군** — 가만히 서 있으면 300프레임 동안 **한 번도 안 바뀐다.**

## ★ 「되돌아오는가」를 반드시 봐라

```
f1   P1반전=0  P2반전=1   (넘기 전)
f98  P1반전=0  P2반전=0   (P1 이 P2 를 지난다 — P2 가 먼저 돈다)
f142 P1반전=1  P2반전=0   (P1 착지 — 여기서 돈다)
f278 P1반전=1  P2반전=1   (되넘는 중)
f322 P1반전=0  P2반전=1   ← **원래대로 돌아온다**
```

KOF 는 `0x0D4A` 를 반전이라 잘못 잡았는데 그건 **한 번 서면 안 내려오는 플래그**였고,
그 탓에 배포된 엔진이 한 라운드 **두 번째 발동부터 커맨드를 좌우로 뒤집었다.**
스냅숏 두 장으로는 그 둘이 구별이 안 된다. **되돌아오는 것까지 봐야 반전이다.**

## 엔진에 넣은 방식

매크로 대본은 방향을 `F`(앞)·`B`(뒤)로 적고, **나갈 때** `lb_fwd()` 가 반전을 읽어
실제 비트로 바꾼다. **매크로가 도는 중에는 시작할 때의 앞을 끝까지 쓴다** —
중간에 반전이 바뀌면 커맨드가 꺾여 아무것도 안 나가기 때문이다(단위 시험에 있다).

## 관문 — 갱신된 `lb_m2gate.py`

```
위상 0 · 엔진 켬 : 20/20      위상 0 · 넘어간 뒤: 20/20
위상 1 · 엔진 켬 : 20/20      위상 1 · 넘어간 뒤: 20/20
     엔진 끔 대조군:  0/20
연속 발동(세이브 안 불러옴) 5/5
판정: PASS
```

단위 시험도 늘렸다: **반대편에서 앞이 L 로 뒤집히는 18프레임 시간표**와
**도는 중에 반전이 바뀌어도 시작할 때의 앞을 쓰는지**. 에뮬로는 비트를 못 본다.

미측정 상수 **5개** 남음(`OFF_H1`·`OFF_HP2`·`OFF_COMBO`·강약 문턱·커맨드 창).

## 다음 (M3 ②③)

② 링 주입 — `0x1313~`, 머리 `0x1312`. 지금 18프레임 지연을 3 수준으로.
　★ **월화 링은 값이 안 밀리고 머리만 나아간다.** SvC·KOF 의 밀기를 흉내 내지 마라.
③ 슬롯 일곱(N/F/B/D/DF/DB/AIR). 공중은 `OFF_H1`(지상/공중)이 필요하니 그것부터 재야 한다.
